---
---

<section class="offered-services section">
  <div class="container">
    <div class="offered-services__header">
      <p class="offered-services__eyebrow">What We Offer</p>
      <h2 class="offered-services__title">Designed to elevate your brand at every stage</h2>
      <p class="offered-services__intro">
        Choose a trusted brand expert who has launched over 100 successful brands.
        We bring a strategic approach, meticulous attention to detail, and a
        commitment to delivering work that lasts.
      </p>
    </div>
  </div>
</section>

<style>
  .offered-services {
    background-color: var(--color-white);
  }

  .offered-services__header {
    text-align: center;
    max-width: 600px;
    margin-inline: auto;
  }

  .offered-services__eyebrow {
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--color-gold);
    margin-bottom: var(--space-sm);
    text-align: center;
  }

  .offered-services__title {
    margin-bottom: var(--space-md);
  }

  .offered-services__intro {
    color: var(--color-warm-neutral);
    font-size: 1.05rem;
    line-height: 1.8;
    margin-inline: auto;
    max-width: none;
  }
</style>
